---
description: Add a year-priority todo. Appends under `## active` in wiki/todos/year.md.
---

Append the following todo to `wiki/todos/year.md` under the `## active` section. If the file doesn't exist, create it with standard frontmatter (`type: todo-list`, title `"[todo] year-priority"`). If the `## active` section doesn't exist, create it.

Format the todo as:

```
- [ ] <text> (created: <today-ISO-date>)
```

Don't mint a `(todo:TN)` ID — the next CURATE wave's sync-todos sweep mints IDs.

Todo content:

$ARGUMENTS
